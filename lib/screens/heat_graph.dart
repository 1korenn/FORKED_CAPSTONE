import 'package:flutter/material.dart';

class HeatGraphScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Heat Graph Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
