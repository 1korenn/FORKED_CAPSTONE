import 'package:flutter/material.dart';

class PhLevelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PH Level'),
      ),
      body: Center(
        child: Text('PH Level Screen'),
      ),
    );
  }
}