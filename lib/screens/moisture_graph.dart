import 'package:flutter/material.dart';

class MoistureGraphScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Moisture Graph Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
